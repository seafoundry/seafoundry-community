name: tier_analyzer_plugin
description: Tier enforcement analyzer plugin for SeaFoundry.
publish_to: 'none'

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  analyzer: '>=7.5.1 <11.0.0'
  analyzer_plugin: '>=0.13.4 <1.0.0'
  glob: ^2.1.2
  path: ^1.9.0
  yaml: ^3.1.2

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0

analyzer_plugin:
  entrypoint: bin/plugin.dart
